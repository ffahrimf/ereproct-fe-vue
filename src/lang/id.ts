import { GenericObject } from "../interface/composable.interface";

const id: GenericObject = {
  tes: "asd",
  delete_confirmation:
    "Apakah Anda yakin ingin menghapus data ini? Tindakan ini tidak dapat dikembalikan."
};

export default id;
